---
import { Image } from "astro:assets";

interface Props {
  name: string;
  img: Promise<any>;
  url: string;
}

const { name, img, url } = Astro.props;
---

<div class="container">
  <a href={url}>
    <Image
      src={img}
      alt=`${name} Thumbnail`
      title={name}
      width={368}
      height={207}
    />
  </a>
</div>

<style>
  .container {
    width: auto;
    height: auto;
    aspect-ratio: 16/9;
    overflow: hidden;
  }

  img {
    border-radius: 0.5em;
  }
</style>