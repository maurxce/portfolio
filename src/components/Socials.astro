---
import Icon from "./Icon.astro";
import socials from "../config/socials";
---

<div class="container">
  {socials.map(social => (
    <Icon social={social} />
  ))}
</div>

<style>
  .container {
    display: flex;
    justify-content: space-evenly;
  }
</style>