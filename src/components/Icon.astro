---
import { Image } from "astro:assets";

interface Props {
  name: string;
  image: Promise<any>;
  url: string;
}

const { name, image, url }: Props  = Astro.props;
---

<a href={url}>
  <Image
    src={image}
    alt=`${name} Icon`
    title={name}
    width={64}
    height={64}
  />
</a>

<style>
  img {
    border-radius: 50%;
  }
</style>